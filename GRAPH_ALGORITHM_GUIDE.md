# 道路网络连接算法指南

## 概述

RoadArchitect 现在支持多种图连接算法，用于生成不同密度和风格的道路网络。

## 可用算法

### 1. Gabriel 图 (推荐) 🌟
- **特点**: 蜂窝状网络，平衡了连通性和稀疏性
- **适用场景**: 大多数情况下的最佳选择
- **边数**: 中等（约 3-4 条边/节点）
- **算法**: 如果以两个节点为直径的圆内不包含其他节点，则连接这两个节点

### 2. 相对邻域图 (RNG)
- **特点**: 非常稀疏的网络
- **适用场景**: 需要最少道路的场景
- **边数**: 较少（约 2-3 条边/节点）
- **算法**: 只连接"相对接近"的节点对

### 3. K 最近邻 (KNN)
- **特点**: 每个节点连接固定数量的最近邻居
- **适用场景**: 需要可预测的连接数量
- **边数**: 固定（K=3，约 3 条边/节点）
- **算法**: 每个节点连接到最近的 K 个邻居

### 4. 传统模式 (Legacy)
- **特点**: 密集网络，连接所有可能的节点
- **适用场景**: 需要最大连通性的场景
- **边数**: 很多（可能 10+ 条边/节点）
- **算法**: 连接所有在范围内且不交叉的节点对

## 算法对比

| 算法 | 边数 | 计算速度 | 网络密度 | 推荐度 |
|------|------|----------|----------|--------|
| Gabriel | 中等 | 快 | 适中 | ⭐⭐⭐⭐⭐ |
| RNG | 少 | 中等 | 稀疏 | ⭐⭐⭐⭐ |
| KNN | 固定 | 快 | 可控 | ⭐⭐⭐ |
| Legacy | 很多 | 慢 | 密集 | ⭐⭐ |

## 使用方法

### 自动应用（新节点）

当添加新节点时，系统会自动使用 **Gabriel 图算法** 重新计算所有连接。

### 手动重建网络

如果你想对现有网络应用不同的算法，可以：

1. 打开游戏控制台
2. 使用命令（待实现）：
   ```
   /roadarchitect rebuild <algorithm>
   ```
   其中 `<algorithm>` 可以是：
   - `gabriel` - Gabriel 图（推荐）
   - `rng` - 相对邻域图
   - `knn` - K 最近邻
   - `legacy` - 传统模式

## 技术细节

### Gabriel 图算法
```
对于每对节点 A 和 B：
  如果以 AB 为直径的圆内不包含其他节点 C
    则连接 A 和 B
```

### 相对邻域图算法
```
对于每对节点 A 和 B：
  如果不存在节点 C 使得 dist(A,C) < dist(A,B) 且 dist(B,C) < dist(A,B)
    则连接 A 和 B
```

### K 最近邻算法
```
对于每个节点 A：
  找到距离最近的 K 个节点
  连接 A 到这些节点
```

## 性能考虑

- **Gabriel 图**: O(n³) 时间复杂度，但对于典型的节点数量（<100）非常快
- **RNG**: O(n³) 时间复杂度，与 Gabriel 图相似
- **KNN**: O(n² log n) 时间复杂度，最快的算法
- **Legacy**: O(n²) 时间复杂度，但会生成更多边

## 示例效果

### 密集网络（Legacy）
```
A---B---C
|\ /|\ /|
| X | X |
|/ \|/ \|
D---E---F
```

### 蜂窝网络（Gabriel）
```
A---B---C
|   |   |
D---E---F
```

### 稀疏网络（RNG）
```
A---B   C
    |   |
D   E---F
```

## 建议

1. **首次使用**: 使用默认的 Gabriel 图算法
2. **道路太多**: 切换到 RNG 算法
3. **道路太少**: 切换到 KNN 或 Legacy 算法
4. **性能问题**: 使用 KNN 算法（最快）

## 未来改进

- [ ] 添加配置选项来选择默认算法
- [ ] 添加游戏内命令来重建网络
- [ ] 添加可视化工具来比较不同算法
- [ ] 支持混合算法（例如：Gabriel + 最小生成树）
